// Configuration flags for the app
export const CONFIG = {
  // Set to true to bypass the 300-meter radius check for check-ins (for testing purposes only)
  DEBUG_BYPASS_LOCATION_CHECK: false,
  
  // Default check-in radius in meters
  DEFAULT_CHECKIN_RADIUS: 300
};
